import { SatishDirective } from './satish.directive';

describe('SatishDirective', () => {
  it('should create an instance', () => {
    const directive = new SatishDirective();
    expect(directive).toBeTruthy();
  });
});
